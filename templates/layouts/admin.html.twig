{% set current_locale = app.request.locale|default('fa') %}
{% set text_direction = current_locale == 'fa' ? 'rtl' : 'ltr' %}
<!DOCTYPE html>
<html lang="{{ current_locale }}" dir="{{ text_direction }}" data-locale-dir="{{ text_direction }}">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{% block title %}{{ 'app.name'|trans }}{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/svg+xml" href="{{ asset('/favicon.svg') }}">
    <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('assets/css/perfect-scrollbar.min.css') }}">
    <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('assets/css/style.css') }}">
    <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('assets/css/animate.css') }}">
    {% block stylesheets %}{% endblock %}
    <script src="{{ asset('assets/js/perfect-scrollbar.min.js') }}"></script>
    <script defer src="{{ asset('assets/js/popper.min.js') }}"></script>
    <script defer src="{{ asset('assets/js/tippy-bundle.umd.min.js') }}"></script>
    <script defer src="{{ asset('assets/js/sweetalert.min.js') }}"></script>
</head>
<body
    x-data="main"
    class="antialiased relative font-vazirmatn text-sm font-normal overflow-x-hidden bg-[#fafafa] dark:bg-[#060818]"
    :class="[ $store.app.sidebar ? 'toggle-sidebar' : '', $store.app.theme === 'dark' || $store.app.isDarkMode ? 'dark' : '', $store.app.menu, $store.app.layout, $store.app.rtlClass ]"
>
    <div
        x-cloak
        class="fixed inset-0 bg-[black]/60 z-50 lg:hidden"
        :class="{ 'hidden': !$store.app.sidebar }"
        @click="$store.app.toggleSidebar()"
    ></div>

    <div class="main-container text-black dark:text-white-dark min-h-screen" :class="[$store.app.navbar]">
        {% include 'partials/admin/_sidebar.html.twig' %}

        <div class="main-content flex min-h-screen flex-col">
            {% include 'partials/admin/_navbar.html.twig' %}

            <main class="flex-1 p-6 animate__animated" :class="[$store.app.animation]">
                {% include 'partials/admin/_flash_messages.html.twig' %}

                {% set header_title %}
                    {% block page_title %}{% endblock %}
                {% endset %}

                {% set header_subtitle %}
                    {% block page_subtitle %}{% endblock %}
                {% endset %}

                {% set header_actions %}
                    {% block page_actions %}{% endblock %}
                {% endset %}

                {% block page_header %}
                    {% if header_title|trim or header_subtitle|trim or header_actions|trim %}
                        {% include 'partials/admin/_page_header.html.twig' with {
                            title: header_title|trim,
                            subtitle: header_subtitle|trim,
                            actions: header_actions|trim,
                        } %}
                    {% endif %}
                {% endblock %}

                {% block body %}{% endblock %}
            </main>

            {% include 'components/common/footer.html.twig' %}
        </div>
    </div>

    <script src="{{ asset('assets/js/alpine-collaspe.min.js') }}"></script>
    <script src="{{ asset('assets/js/alpine-persist.min.js') }}"></script>
    <script defer src="{{ asset('assets/js/alpine.min.js') }}"></script>
    <script src="{{ asset('assets/js/custom.js') }}"></script>
    <script defer src="{{ asset('assets/js/alpine-ui.min.js') }}"></script>
    <script defer src="{{ asset('assets/js/alpine-focus.min.js') }}"></script>
    {% block javascripts %}
        {{ importmap('app') }}
    {% endblock %}
</body>
</html>
