{% extends 'layouts/admin.html.twig' %}

{% block page_title %}{{ 'activity.offer.title'|trans }}{% endblock %}
{% block page_subtitle %}{{ activity.name }}{% endblock %}
{% block page_actions %}
    <a href="{{ path('activity_edit', { id: activity.id }) }}" class="btn btn-light">{{ 'activity.offer.back_to_activity'|trans }}</a>
    <a href="{{ path('activity_offer_new', { activity: activity.id }) }}" class="btn btn-primary">{{ 'activity.offer.new'|trans }}</a>
{% endblock %}

{% block body %}
    {% if offers is empty %}
        <div class="rounded border border-white-light/60 bg-white p-10 text-center text-white-dark dark:border-dark-light/20 dark:bg-[#0e1726]">
            {{ 'activity.offer.empty'|trans }}
        </div>
    {% else %}
        <div class="table-responsive">
            <table class="table-hover">
                <thead>
                    <tr>
                        <th>{{ 'activity.offer.date_context'|trans }}</th>
                        <th>{{ 'activity.offer.price'|trans }}</th>
                        <th>{{ 'activity.offer.participants'|trans }}</th>
                        <th>{{ 'activity.offer.availability_status'|trans }}</th>
                        <th>{{ 'activity.offer.priority'|trans }}</th>
                        <th>{{ 'activity.offer.active'|trans }}</th>
                        <th>{{ 'activity.common.actions'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for offer in offers %}
                        <tr>
                            <td>
                                {% if offer.specificDate %}
                                    {{ offer.specificDate|date('Y-m-d') }}
                                {% elseif offer.validFrom or offer.validTo %}
                                    {{ offer.validFrom ? offer.validFrom|date('Y-m-d') : '-' }} &rarr; {{ offer.validTo ? offer.validTo|date('Y-m-d') : '-' }}
                                {% else %}
                                    {{ 'activity.offer.always_valid'|trans }}
                                {% endif %}
                            </td>
                            <td>{{ offer.priceLabel }}</td>
                            <td>{{ offer.minimumParticipants ?: '-' }} / {{ offer.maximumParticipants ?: '-' }}</td>
                            <td>{{ ('activity.availability_status.' ~ offer.availabilityStatus.value)|trans }}</td>
                            <td>{{ offer.priority }}</td>
                            <td>
                                <span class="badge {{ offer.active ? 'bg-success' : 'bg-danger' }} text-white">{{ offer.active ? 'activity.offer.active'|trans : 'activity.offer.inactive'|trans }}</span>
                            </td>
                            <td>
                                <div class="flex flex-wrap gap-2">
                                    <a href="{{ path('activity_offer_edit', { activity: activity.id, offer: offer.id }) }}" class="btn btn-sm btn-outline-primary">{{ 'activity.common.edit'|trans }}</a>
                                    <form method="post" action="{{ path('activity_offer_toggle', { activity: activity.id, offer: offer.id }) }}">
                                        <input type="hidden" name="_token" value="{{ csrf_token('toggle_activity_offer_' ~ offer.id) }}">
                                        <button type="submit" class="btn btn-sm {{ offer.active ? 'btn-outline-danger' : 'btn-outline-success' }}">{{ offer.active ? 'activity.offer.deactivate'|trans : 'activity.offer.activate'|trans }}</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
{% endblock %}
