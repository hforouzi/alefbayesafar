{% extends 'layouts/admin.html.twig' %}

{% block page_title %}{{ 'destination.state.title'|trans }}{% endblock %}
{% block page_subtitle %}{{ 'destination.state.subtitle'|trans }}{% endblock %}
{% block page_actions %}
    <a href="{{ path('destination_state_new') }}" class="btn btn-primary">{{ 'destination.state.new'|trans }}</a>
{% endblock %}

{% block body %}
    {% include '@Destination/admin/_table_filters.html.twig' with { table: 'states' } %}

    {% set table_body %}
        <thead>
            <tr>
                <th>{{ 'destination.state.name'|trans }}</th>
                <th>{{ 'destination.state.name_fa'|trans }}</th>
                <th>{{ 'destination.state.country'|trans }}</th>
                <th>{{ 'destination.state.code'|trans }}</th>
                <th>{{ 'destination.common.active'|trans }}</th>
                <th class="text-right">{{ 'common.actions'|trans }}</th>
            </tr>
        </thead>
        <tbody>
            {% for state in states %}
                <tr>
                    <td class="font-semibold text-black dark:text-white-light">{{ state.name }}</td>
                    <td>{{ state.nameFa ?: '-' }}</td>
                    <td>{{ state.country }}</td>
                    <td>{{ state.code }}</td>
                    <td>
                        {% include 'components/_status_badge.html.twig' with {
                            status: state.active ? 'active' : 'inactive',
                            label: state.active ? 'destination.common.active' : 'destination.common.inactive',
                        } %}
                    </td>
                    <td class="whitespace-nowrap text-right">
                        <div class="flex flex-wrap justify-end gap-2">
                            <a href="{{ path('destination_state_edit', { id: state.id }) }}" class="btn btn-sm btn-outline-primary">{{ 'common.edit'|trans }}</a>
                            <form method="post" action="{{ path('destination_state_delete', { id: state.id }) }}" onsubmit="return confirm('{{ 'destination.common.delete_confirmation'|trans }}');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete_destination_state_' ~ state.id) }}">
                                <button type="submit" class="btn btn-sm btn-outline-danger">{{ 'common.delete'|trans }}</button>
                            </form>
                        </div>
                    </td>
                </tr>
            {% else %}
                <tr><td colspan="6" class="py-10 text-center text-white-dark"><div>{{ 'destination.datatable.no_results'|trans }}</div><a href="{{ path('destination_state_index') }}" class="mt-3 inline-flex text-primary hover:underline">{{ 'destination.datatable.reset_filters'|trans }}</a></td></tr>
            {% endfor %}
        </tbody>
    {% endset %}
    {% include 'components/_card.html.twig' with { body: include('components/_table.html.twig', { body: table_body }) } %}
    {% include '@Destination/admin/_pagination.html.twig' %}
{% endblock %}
