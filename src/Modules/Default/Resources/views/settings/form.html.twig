{% extends "components/layout/default.html.twig" %}
{% block content %}
    <h1>Create Setting</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Save</button>
    {{ form_end(form) }}
{% endblock %}
